#include <stdio.h>



struct hour // estrutura global
{
	int hours;
	int minutes;
	int seconds;
};

void recHours(struct hour [5]){

	int, a,b,c;

	for(x = ; x < 5; x++){
		printf("\nHours -> ");
		scanf("%d", &);
		printf("\nMinutes -> ");
		scanf();
		printf("\nSeconds -> ");
		scanf();
}


}

void printHour(struct hour [5]){


}

int main(void){

struct hour list[5];
int x;

recHours(list);
printHour(list);


return 0;
}